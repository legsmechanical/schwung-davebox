/* Seam registry — resident ui.js helpers that extracted modules still need.
 * ui.js populates R at load; every entry is an IOU dissolved by Phase 6
 * (ui_dsp_bridge / record module). Do NOT add entries without documenting
 * the owning future module. */
export const R = {};
